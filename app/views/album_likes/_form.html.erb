<%= form_with(model: album_like, local: true) do |form| %>
  <% if album_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album_like.errors.count, "error") %> prohibited this album_like from being saved:</h2>

      <ul>
      <% album_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :album_like_user_id %>
  </div>

  <div class="field">
    <%= form.label :album_id %>
    <%= form.number_field :album_id, id: :album_like_album_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
